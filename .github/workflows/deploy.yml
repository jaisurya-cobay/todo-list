name: Deploy to Production

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      RELEASE_DATE: ""
      RELEASE_TAG: ""
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set release variables
        run: |
          echo "RELEASE_DATE=$(date +%Y-%m-%d)" >> "$GITHUB_ENV"
          echo "RELEASE_TAG=release-$(date +%Y-%m-%d)-${{ github.run_number }}" >> "$GITHUB_ENV"
          echo "SHORT_SHA=${GITHUB_SHA:0:7}" >> "$GITHUB_ENV"

      - name: Build
        run: echo "Building..."

      - name: Deploy
        run: echo "Deploying to production..."

      - name: Extract Jira issue keys from commits
        id: issues
        run: |
          # Get all commits since last tag (or all commits if no tag)
          LAST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
          if [ -n "$LAST_TAG" ]; then
            COMMITS=$(git log "${LAST_TAG}..HEAD" --pretty=format:'%s %b')
          else
            COMMITS=$(git log -10 --pretty=format:'%s %b')
          fi

          KEYS=$(echo "$COMMITS" | grep -oE '[A-Z]+-[0-9]+' | sort -u | tr '\n' ' ')

          if [ -z "$KEYS" ]; then
            echo "No Jira issue keys found."
            echo "found=false" >> "$GITHUB_OUTPUT"
          else
            echo "Found issue keys: ${KEYS}"
            echo "keys=${KEYS}" >> "$GITHUB_OUTPUT"
            echo "found=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Transition issues to Prod
        if: steps.issues.outputs.found == 'true'
        env:
          JIRA_BASE_URL: ${{ secrets.JIRA_BASE_URL }}
          JIRA_EMAIL: ${{ secrets.JIRA_EMAIL }}
          JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
        run: |
          REPO="${{ github.repository }}"

          PROD_TRANSITION_ID="51"

          for KEY in ${{ steps.issues.outputs.keys }}; do
            echo "Processing ${KEY}..."

            # Transition to Prod
            curl -s -u "${JIRA_EMAIL}:${JIRA_API_TOKEN}" \
              -H "Content-Type: application/json" \
              -X POST \
              "${JIRA_BASE_URL}/rest/api/3/issue/${KEY}/transitions" \
              -d "{\"transition\":{\"id\":\"${PROD_TRANSITION_ID}\"}}"

            # Add deployment comment
            curl -s -u "${JIRA_EMAIL}:${JIRA_API_TOKEN}" \
              -H "Content-Type: application/json" \
              -X POST \
              "${JIRA_BASE_URL}/rest/api/3/issue/${KEY}/comment" \
              -d "{\"body\":{\"type\":\"doc\",\"version\":1,\"content\":[{\"type\":\"paragraph\",\"content\":[{\"type\":\"text\",\"text\":\"Deployed to production (${SHORT_SHA})\"},{\"type\":\"hardBreak\"},{\"type\":\"text\",\"text\":\"Repo: ${REPO}\"}]}]}}"

            echo "  -> ${KEY} transitioned to Prod"
          done

          echo "### Jira Issues in Production" >> "$GITHUB_STEP_SUMMARY"
          echo "Issues: ${{ steps.issues.outputs.keys }}" >> "$GITHUB_STEP_SUMMARY"

      - name: Create Jira Release
        if: steps.issues.outputs.found == 'true'
        id: jira_release
        env:
          JIRA_BASE_URL: ${{ secrets.JIRA_BASE_URL }}
          JIRA_EMAIL: ${{ secrets.JIRA_EMAIL }}
          JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
        run: |
          VERSION_NAME="${RELEASE_TAG}"
          PROJECT_ID="10208"

          # Create Jira version
          VERSION_RESPONSE=$(curl -s -w "\n%{http_code}" \
            -u "${JIRA_EMAIL}:${JIRA_API_TOKEN}" \
            -H "Content-Type: application/json" \
            -X POST \
            "${JIRA_BASE_URL}/rest/api/3/version" \
            -d "{
              \"name\": \"${VERSION_NAME}\",
              \"projectId\": ${PROJECT_ID},
              \"released\": true,
              \"releaseDate\": \"${RELEASE_DATE}\",
              \"description\": \"Automated release from GitHub Actions run #${{ github.run_number }}\"
            }")

          HTTP_CODE=$(echo "$VERSION_RESPONSE" | tail -1)
          BODY=$(echo "$VERSION_RESPONSE" | sed '$d')
          VERSION_ID=$(echo "$BODY" | jq -r '.id // empty')

          if [ -n "$VERSION_ID" ]; then
            echo "Created Jira version: ${VERSION_NAME} (ID: ${VERSION_ID})"
            echo "version_id=${VERSION_ID}" >> "$GITHUB_OUTPUT"
            echo "version_name=${VERSION_NAME}" >> "$GITHUB_OUTPUT"
          else
            echo "::warning::Failed to create Jira version (HTTP ${HTTP_CODE}): ${BODY}"
          fi

      - name: Link issues to Jira Release
        if: steps.jira_release.outputs.version_id != ''
        env:
          JIRA_BASE_URL: ${{ secrets.JIRA_BASE_URL }}
          JIRA_EMAIL: ${{ secrets.JIRA_EMAIL }}
          JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
        run: |
          VERSION_NAME="${{ steps.jira_release.outputs.version_name }}"

          for KEY in ${{ steps.issues.outputs.keys }}; do
            curl -s -u "${JIRA_EMAIL}:${JIRA_API_TOKEN}" \
              -H "Content-Type: application/json" \
              -X PUT \
              "${JIRA_BASE_URL}/rest/api/3/issue/${KEY}" \
              -d "{\"update\":{\"fixVersions\":[{\"add\":{\"name\":\"${VERSION_NAME}\"}}]}}"

            echo "  -> ${KEY} linked to ${VERSION_NAME}"
          done

      - name: Collect merged PRs since last release
        id: prs
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          LAST_TAG=$(git describe --tags --abbrev=0 HEAD^ 2>/dev/null || echo "")

          if [ -n "$LAST_TAG" ]; then
            # Get tag timestamp as Unix epoch then convert to UTC ISO format
            TAG_EPOCH=$(git log -1 --format=%at "$LAST_TAG")
            SINCE_DATE=$(date -u -d "@${TAG_EPOCH}" +%Y-%m-%dT%H:%M:%SZ 2>/dev/null || date -u -r "${TAG_EPOCH}" +%Y-%m-%dT%H:%M:%SZ)
          else
            SINCE_DATE="2000-01-01T00:00:00Z"
          fi
          echo "Collecting PRs since: ${SINCE_DATE} (tag: ${LAST_TAG})"

          # Get feature PRs merged to develop since last release (these are the actual work)
          DEVELOP_PRS=$(gh pr list --base develop --state merged --limit 100 \
            --json number,title,author,mergedAt,headRefName \
            --jq "[.[] | select(.mergedAt > \"${SINCE_DATE}\")]")

          # Also get feature PRs merged directly to main (not from develop)
          MAIN_PRS=$(gh pr list --base main --state merged --limit 100 \
            --json number,title,author,mergedAt,headRefName \
            --jq "[.[] | select(.mergedAt > \"${SINCE_DATE}\" and .headRefName != \"develop\")]")

          # Combine and deduplicate by PR number
          PR_JSON=$(echo "${DEVELOP_PRS}${MAIN_PRS}" | jq -s 'add | unique_by(.number) | sort_by(.mergedAt)')

          echo "Found $(echo "$PR_JSON" | jq 'length') PRs"

          # Build grouped notes by author
          AUTHORS=$(echo "$PR_JSON" | jq -r '.[].author.login' | sort -u)

          GROUPED_NOTES=""
          PR_SUMMARY=""
          for AUTHOR in $AUTHORS; do
            GROUPED_NOTES+="### @${AUTHOR}\n"
            while IFS='|' read -r NUM TITLE BRANCH; do
              GROUPED_NOTES+="- #${NUM} ${TITLE}"
              # Extract Jira key from branch name
              JIRA_KEY=$(echo "$BRANCH" | grep -oE '[A-Z]+-[0-9]+' | head -1)
              if [ -n "$JIRA_KEY" ]; then
                GROUPED_NOTES+=" (${JIRA_KEY})"
              fi
              GROUPED_NOTES+="\n"
              PR_SUMMARY+="• #${NUM} ${TITLE} (by ${AUTHOR})\n"
            done < <(echo "$PR_JSON" | jq -r ".[] | select(.author.login == \"${AUTHOR}\") | \"\(.number)|\(.title)|\(.headRefName)\"")
            GROUPED_NOTES+="\n"
          done

          # Save for later steps
          echo "grouped_notes<<EOFNOTES" >> "$GITHUB_OUTPUT"
          echo -e "$GROUPED_NOTES" >> "$GITHUB_OUTPUT"
          echo "EOFNOTES" >> "$GITHUB_OUTPUT"

          echo "pr_summary<<EOFSUMMARY" >> "$GITHUB_OUTPUT"
          echo -e "$PR_SUMMARY" >> "$GITHUB_OUTPUT"
          echo "EOFSUMMARY" >> "$GITHUB_OUTPUT"

          PR_COUNT=$(echo "$PR_JSON" | jq 'length')
          AUTHOR_COUNT=$(echo "$AUTHORS" | wc -w | tr -d ' ')
          echo "pr_count=${PR_COUNT}" >> "$GITHUB_OUTPUT"
          echo "author_count=${AUTHOR_COUNT}" >> "$GITHUB_OUTPUT"

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          JIRA_BASE_URL: ${{ secrets.JIRA_BASE_URL }}
        run: |
          KEYS="${{ steps.issues.outputs.keys }}"

          NOTES="## 📦 Release Summary\n\n"
          NOTES+="**PRs:** ${{ steps.prs.outputs.pr_count }} | **Contributors:** ${{ steps.prs.outputs.author_count }}\n\n"

          NOTES+="## 👥 Changes by Developer\n\n"
          NOTES+="${{ steps.prs.outputs.grouped_notes }}\n"

          if [ -n "$KEYS" ]; then
            NOTES+="## 🎫 Jira Issues\n\n"
            for KEY in $KEYS; do
              NOTES+="- [${KEY}](${JIRA_BASE_URL:-https://cobay.atlassian.net}/browse/${KEY})\n"
            done
          fi

          NOTES+="\n**Deployed:** ${RELEASE_DATE}"

          git tag "${RELEASE_TAG}"
          git push origin "${RELEASE_TAG}"

          gh release create "${RELEASE_TAG}" \
            --title "Release ${RELEASE_DATE} (#${{ github.run_number }})" \
            --notes "$(echo -e "$NOTES")" \
            --latest

          echo "### Release Created" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Tag:** ${RELEASE_TAG}" >> "$GITHUB_STEP_SUMMARY"
          echo "- **PRs included:** ${{ steps.prs.outputs.pr_count }}" >> "$GITHUB_STEP_SUMMARY"

      - name: Notify Slack releases-test channel
        if: always()
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.RELEASE_SLACK_WEBHOOK_URL }}
          JIRA_BASE_URL: ${{ secrets.JIRA_BASE_URL }}
        run: |
          TAG="${RELEASE_TAG}"
          IST_TIME=$(TZ='Asia/Kolkata' date "+%b %d, %Y %I:%M %p IST")
          UTC_TIME=$(date -u "+%b %d, %Y %I:%M %p UTC")
          KEYS="${{ steps.issues.outputs.keys }}"
          STATUS="${{ job.status }}"
          RUN_URL="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"

          if [ "$STATUS" = "success" ]; then
            HEADER="🚀  Release Deployed to Production"
            STATUS_TEXT="SUCCESS"
            URGENCY="✅  *Deployment completed successfully!*"
          else
            HEADER="⚠️  Release Deployment Failed"
            STATUS_TEXT="FAILED"
            URGENCY="🔴  *Deployment encountered errors. Check GitHub Actions.*"
          fi

          # Build Jira links
          JIRA_LINKS=""
          if [ -n "$KEYS" ]; then
            for KEY in $KEYS; do
              JIRA_LINKS+="<${JIRA_BASE_URL:-https://cobay.atlassian.net}/browse/${KEY}|${KEY}> "
            done
          else
            JIRA_LINKS="None"
          fi

          # Build PR summary for Slack
          PR_COUNT="${{ steps.prs.outputs.pr_count }}"
          AUTHOR_COUNT="${{ steps.prs.outputs.author_count }}"
          PR_SUMMARY="${{ steps.prs.outputs.pr_summary }}"
          if [ -z "$PR_SUMMARY" ]; then
            PR_SUMMARY="No PRs found"
          fi

          curl -s -X POST -H "Content-Type: application/json" \
            -d "{
              \"blocks\": [
                {
                  \"type\": \"header\",
                  \"text\": {
                    \"type\": \"plain_text\",
                    \"text\": \"${HEADER}\"
                  }
                },
                {
                  \"type\": \"section\",
                  \"text\": {
                    \"type\": \"mrkdwn\",
                    \"text\": \"${URGENCY}\"
                  }
                },
                {\"type\": \"divider\"},
                {
                  \"type\": \"section\",
                  \"fields\": [
                    {\"type\": \"mrkdwn\", \"text\": \"*📦 Repository:*\n<https://github.com/${{ github.repository }}|${{ github.repository }}>\"},
                    {\"type\": \"mrkdwn\", \"text\": \"*🏷️ Tag:*\n\`${TAG}\`\"},
                    {\"type\": \"mrkdwn\", \"text\": \"*🔀 Commit:*\n\`${SHORT_SHA}\`\"},
                    {\"type\": \"mrkdwn\", \"text\": \"*👤 Triggered by:*\n${{ github.actor }}\"},
                    {\"type\": \"mrkdwn\", \"text\": \"*🔔 Status:*\n\`${STATUS_TEXT}\`\"},
                    {\"type\": \"mrkdwn\", \"text\": \"*🎫 Jira Issues:*\n${JIRA_LINKS}\"}
                  ]
                },
                {\"type\": \"divider\"},
                {
                  \"type\": \"section\",
                  \"text\": {
                    \"type\": \"mrkdwn\",
                    \"text\": \"*📋 Release Summary: ${PR_COUNT} PRs from ${AUTHOR_COUNT} contributors*\n\n${PR_SUMMARY}\"
                  }
                },
                {\"type\": \"divider\"},
                {
                  \"type\": \"context\",
                  \"elements\": [
                    {
                      \"type\": \"mrkdwn\",
                      \"text\": \"⏰ *${IST_TIME}*  |  🌐 *${UTC_TIME}*  |  🔢 *Run #${{ github.run_number }}*\"
                    }
                  ]
                },
                {
                  \"type\": \"actions\",
                  \"elements\": [
                    {\"type\": \"button\", \"text\": {\"type\": \"plain_text\", \"text\": \"View Run\"}, \"url\": \"${RUN_URL}\"},
                    {\"type\": \"button\", \"text\": {\"type\": \"plain_text\", \"text\": \"GitHub Release\"}, \"url\": \"https://github.com/${{ github.repository }}/releases/tag/${TAG}\"}
                  ]
                }
              ]
            }" "$SLACK_WEBHOOK_URL"
